#/bin/bash
LOCAL_PATH=~/template-worker/devutils/deploy
cd ~/ && ibl db new seed "${LOCAL_PATH}/seed.iblcli-seed" --db antiraid --extensions uuid-ossp\|citext # && ibl db gen-ci-schema ${LOCAL_PATH}/seed-ci.json
